# input()函数的工作原理
input()函数让程序暂停运行，等待用户输入一些文本。获得用户输入后，Python将其赋给一个变量，以便使用。
```python
message = input("请输入你的名字: ")
print(message)
```
输出结如下：
- ![](assets/Python基础之%20用户输入%20和while循环/file-20260118004234454.png)
- input()函数接受一个参数，即要向用户显示的提示（prompt），让用户知道输入什么样的内容。在这个例子中，当python运行第一行代码时，用户将看到提示：”请输入你的名字“。程序等待用户输入，并在用户回车后继续运行。
# 编写清晰的提示词
每当用户input()函数时，都应该指定清晰易懂的提示，准确地指出希望用户提供什么样的信息--，有时候，提示可能超过一行。例如，你可能需要指出获取特定输入的原因。在这种情况下，可先将提示赋给一个变量，在将这个变量传递给input()函数。这样，即便提示超过一行，input（）语句也会非常清晰。
```python
prompt = "如果你愿意分享你的姓名,请你打出你的名字,如果不愿意分享,"
prompt += "请输入你的别名名: "
messing = input(prompt)
print(messing)

```
这个示例演示了一种创建多行字符串的方式。第一行将消息的前半部分赋给变量prompt。在第二行中，运算符+=在赋给变量prompt的字符串末尾追加一个字符串。
最终的提示占两行，且冒号后面有一个空格，这也使为了使其更加清晰：
程序运行结果如下图：
- ![](assets/Python基础之%20用户输入%20和while循环/file-20260118010128551.png)
# 使用 int() 来获取数值输入

在使用input（）函数时，Python会将用户输入解读为字符串。看下面的示例
- ![](assets/Python基础之%20用户输入%20和while循环/file-20260118224742485.png)
- 用户输入的是数22，但是我们请求Python提供变量的值时，它返回的是‘22’，用户输入的是数值但现在是字符串表示。我们怎么知道这个Python将输入解读成了字符串呢？因为这个数字是用引号引起了的。如果只想打印输入，这一点问题都没有：但如果试图将输入的数进行数学运算，就会出错。如下所示
- ![](assets/Python基础之%20用户输入%20和while循环/file-20260118225333677.png)
- 试图将该输入用于数值比较时，Python会报错，因为它无法将字符串和整数进行比较：不能将赋给age的字符串“22”与数值25进行比较。
- 为了解决这个问题，可使用int()函数将输入的字符串转换为数值，确保能够成功地执行比较操作：
- ![](assets/Python基础之%20用户输入%20和while循环/file-20260118225826486.png)
- 如何在实际的中使用int()函数呢？请看下面的程序
```python
age = input("请输入你的年龄: ")
age = int(age)
if age >= 18:
    print('年轻人加油干')
else:
    print("小朋友,要好号学习哦")
    
```
# 求模运算
在处理数值信息时，求模运算%是个很有用的工具，他将两个数相除并返回余数：
![](assets/Python基础之%20用户输入%20和while循环/file-20260118230800808.png)
求模运算不会指出一个数是另一个数的多少倍，只指出余数是多少。
如果一个数可以被另一个数整除，余数就是0，因此求模运算将会返回0。可以利用这一点来判断一个数是奇数还是偶数：
```python
number = input("请输入任意的整数: ")
number = int(number)
if number % 2 == 0:
    print("你输入的数是一个偶数")
else:
    print("你输入的数是一个奇数")
    
```
# while 循环简介
for循环用于针对集合中的每一个元素执行一个代码块，而while循环则不断地运行，直到指定的条件不在满足为止。
### 使用while循环
可以使用while循环来数数。如下示例
```python
number = 1
while number <= 5:
    print(number)
    number += 1
    
```
执行结果如下：
![](assets/Python基础之%20用户输入%20和while循环/file-20260118232154248.png)
## 让用户选择何时退出
可以使用while循环让程序在用户愿意时不断地运行，如下的程序所示，我们在其中定义一个退出值，只有用户输入的不是这个值，程序将一直运行：
```python
message = "你可以输入任意的信息,我都将给你打印在屏幕上.退出程序请输入quit"
message += "请输入你要打印的信息: "

mesge = ""
while mesge != 'quit':
    mesge = input(message)
    print(mesge)
    
```
这个程序很好，唯一美中不足的地方是，他将单词quit也作为一条消息打印出来了，为了解决这个为题，我们可以用一个if测试来解决这个问题
```python
message = "你可以输入任意的信息,我都将给你打印在屏幕上.退出程序请输入quit"
message += "请输入你要打印的信息: "
mesge = ""
while mesge != 'quit':
    mesge = input(message)
    if mesge != "quit":
        print(mesge)
        
```
## 使用标志
在上一个示例中，我们让程序在满足指定的条件时执行特定的任务。但在更复杂的程序中，有很多不同的条件会导致程序停止运行。在这种情况下，改怎么办呢？
例如，有多种事件可能导致游戏结束，如玩家失去所有飞船，时间已用完，或者要保护的城市陪摧毁。当导致程序结束的事件有很多时，如果在一条while语句中检查所有的这些条件，将即复杂有困难。
	下面在一个程序中添加一个标志，我们把这个标志命名为active（可以任意起名字），用于判断程序是否应该继续运行：
```python
message = "\n你可以输入任意内容,我可以帮你打印在屏幕上,退出程序请输入quit"
message += "\n请输入你的内容: "

active = True
while active:
    messg = input(message)
    if messg != "quit":
        print(messg)
    else:
        active = False
        
```
- 将变了active 设置为true，让程序最初处于活动状态。这样做简化了while语句，应为不需要再其中做任何比较---相关的逻辑由程序的其他部分处理。只要变量active为true，循环就将一直运行。
- 在while循环中，在用户输入后使用一条if语句检查变量messg的值，如果用户输入的是“quit”，就将变量active设置为false，这将导致while循环不在继续执行。如果用户输入的不是“quit”，就将输入的内容作为一条消息打印在屏幕上。
- 这个程序的输出与上一个示例相同。上一个示例讲条件测试直接放在了while语句中，而这个程序使用一个标志来指出程序是否处于活动状态。这样，添加测试（如elif语句）以检查是否发生了其他导致active变为false的事件，就会很容易。在复杂的程序中（比如很多事件会导致程序停止运行的游戏）中，标志很有用：在任意一个事件导致活动标志变成false时，主程序循环将退出。此时可以显示一条游戏结束的消息，并让用户选择是否重玩。
## 使用 break 退出循环
如果不管条件测试的结果如何，想立即退出while循环，不在运行循环中的余下的代码，可以使用break语句。break语句用于控制程序流程，可用来哪些代码将执行，那些代码不执行，从而让程序按你的要求执行你要执行的代码。
```python
message = "\n你可以输入任意内容,我可以帮你打印在屏幕上,退出程序请输入quit"
message += "\n请输入你的内容: "
while True:
    messg = input(message)
    if messg == "quit":
       break
    else:
        print(messg)
        
```

以while true打头的循环将不断运行，直到遇到break语句。这个程序中的循环不断地让用户输入他想输入的内容，直到用户输入“quit”为止。在用户输入“quit”后，将执行break语句，导致python退出循环：
***注意***：在所有python循环中都可以使用break语句。例如，可使用break语句来退出遍历列表或字典的for循环。
## 在循环中使用 continue
要返回循环开头，并根据条件测试结果决定是否继续执行循环，可以使用continue语句，它不像break语句那样不在执行余下的代码并退出整个循环。例如，来看一个从0数到10，只打印其中奇数的循环：
```python
math = 0
while math < 10:
    math += 1
    if math % 2 == 0:
        continue
    print(math)
    
```
代码解释：首先将math这个变量设置为0 ，由于他小于10，python进入while循环。进入循环后，以步长为1的方式网上数，因此math为1.接下来，if语句检查math与2的求模运算结果。如果结果为0 （意味着math可被2整除），就执行continue语句，让python忽略余下的代码，返回到循环的开头。如果当前的数不能被2整除，就执行循环中余下的代码，将这个数打印出来。
### 下面这种写法运行的结果是一样的。如下所示
上面的写法更简单明了
```python
math = 0
while True:
    math += 1
    if math > 10:
        break
    else:
        if math % 2 == 0:
            continue
    print(math)
    
```
## 避免无限死循环
每一个while循环都必须有结束的途径，这样才不会没完没了的执行下去。例如，下面的循环从1数到5：



